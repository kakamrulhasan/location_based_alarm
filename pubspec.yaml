name: flutter_application_17
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^14.0.2
  google_fonts: ^6.3.3
  permission_handler: ^12.0.1
  provider: ^6.1.5+1
  shared_preferences: ^2.5.4
  smooth_page_indicator: ^2.0.1
  geocoding: ^2.0.5
  intl: ^0.18.1
  timezone: ^0.9.3
  awesome_notifications: ^0.8.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
